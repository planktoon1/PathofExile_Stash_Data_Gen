// Define the routes to different destinations

export const enum Directories {
  OUTPUT_DIR_readable = `./output/readable`,
  OUTPUT_DIR_minified = `./output/minified`,
  INPUT_DIR_RePoE = `./input/RePoE_Data`
}
